"""Spot login for scripts/spot_graphnav_goal.py.

Copy this file to ``scripts/spot_credentials.py`` (which is git-ignored) and fill
in your robot account. ``spot_graphnav_goal.py`` imports it; if the import fails
it falls back to the BOSDYN_CLIENT_USERNAME / BOSDYN_CLIENT_PASSWORD env vars,
then to an interactive prompt.
"""

USERNAME = "user"
PASSWORD = "REPLACE_ME"
